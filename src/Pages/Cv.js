import React from 'react';
import Title from '../Components/Title';

const cv = () => {
    return (
        <div>
            <Title text="Mon Parcours" />
        </div>
    );
};

export default cv;